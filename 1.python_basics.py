# Python is an interpreted high-level general-purpose programming language. It can be used for scripting as well

''' language Features that Python has
There are no separate compilation and execution steps like C and C++.nternally, Python converts the source code into an intermediate form called bytecodes which is then translated into native language of specific computer to run it.'''

# first python program.

import os # importing the os module

print("Hello World")

''' A module is a file containing code written by somebody else (usually) which can be imported and used in our programs.
    Built-in modules – Pre-installed in Python
    External modules – Need to install using pip
 Some examples of built-in modules are os, abc, etc. Some examples of external modules are TensorFlow, flask, etc.'''

# multiline print statements
print('''Hello World!
I am Protyush Nayak''')

print(os.listdir()); # listdir() is a method of the os module that gives the files within the same directory as the

# Python for its scope doesn't depend on the braces ( { } ), instead it uses indentation for its scope.


